<h2>Categories</h2>

<div class="collection">
    <% @categories.each do |category| %>
        <%= link_to category.name, category_path(category), class:"collection-item" %>
    <% end %>
</div>

<%= link_to "Create Category", new_category_path, class:"btn blue darken-2" %>
